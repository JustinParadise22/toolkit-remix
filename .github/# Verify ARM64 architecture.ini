# Verify ARM64 architecture
uname -m
# Expected output: aarch64

# Check available disk space (VS Code requires ~200MB)
df -h /

# Verify desktop environment is running
ps aux | grep -E "(gnome|kde|xfce)"

# Verify GUI desktop environment is available
  echo $DISPLAY
# Should return display information like :0 or :10.0

# Check for necessary libraries (example: libX11)
# Flow: emit_site → post_event() → calls all subscribers → collects bool results
# If ANY subscriber returns False → operation blocked
# If ALL subscribers return True → operation proceeds -p | grep libX11.so.6
# Expected output: path to libX11.so.6
# Check for network connectivity
ping -c 4 google.com
# Expected output: 4 packets transmitted, 4 received



# Verify required dependencies (example: libxcb)
ldconfig -p | grep libxcb.so.1
# Expected output: path to libxcb.so.1
# Check for sufficient RAM (VS Code recommends at least 1GB)
free -h
# Expected output: Total memory should be at least 1GB
# Check for CPU information
lscpu



# Expected output: Architecture should be aarch64
# Verify required dependencies (example: libglib-2.0)
ldconfig -p | grep libglib-2.0.so.0
# Expected output: path to libglib-2.0.so.0
# Check for swap space
swapon --show
# Expected output: Swap size should be at least 1GB
# Expected output: CPU architecture should be aarch64
# Check for running X server
pgrep Xorg
# Expected output: Process ID of Xorg if running
# Expected output: Total RAM should be at least 1GB
# Check for internet connectivity
ping -c 4 github.com
# Expected output: 4 packets transmitted, 4 received
# Expected output: Path to libglib-2.0.so.0
# Check for GPU information
lspci | grep -i vga
# Expected output: Information about the GPU if available
# Expected output: Path to libxcb.so.1
# Check for running Wayland session
echo $WAYLAND_DISPLAY
# Should return display information like wayland-0
# Expected output: Swap size should 

# Expected output: Total RAM should
be at least 1GB
# Check for audio functionality
aplay -l
# Expected output: List of audio playback devices
be at least 1GB
# Check for Bluetooth functionality
hciconfig
# Expected output: List of Bluetooth devices if available
# Expected output: Path to libX11.so.6
# Check for printer functionality
lpstat -p
# Expected output: List of configured printers if available be at least 1GB
# Check for webcam functionality

v4l2-ctl --list-devices
# Expected output: List of video capture devices if available
# Expected output: 4 packets transmitted, 4 received
# Check for file system permissions
touch /tmp/vscode_test_file && rm /tmp/vscode_test_file
# Expected output: No errors be at least 1GB
# Check for clipboard functionality
xclip -o < /dev/null
# Expected output: No errors if clipboard is accessible
# Expected output: Process ID of Xorg if running
# Check for keyboard functionality
xset q | grep "Keyboard Control"
# Expected output: Keyboard control settings if accessible
# Expected output: Information about the GPU if available
# Check for mouse functionality
xset q | grep "Pointer Control"
# Expected output: Pointer control settings if accessible
# Expected output: Path to libglib-2.0.so.0
# Check for touchscreen functionality
xinput list | grep -i touchscreen
# Expected output: List of touchscreen devices if available
# Expected output: Path to libxcb.so.1
# Check for multi-monitor setup
xrandr --listmonitors
# Expected output: List of connected monitors if available
# Expected output: Swap size should be at least 1GB
# Check for system updates
sudo apt update && sudo apt upgrade -s
# Expected output: List of available updates if any
# Expected output: Total RAM should be at least 1GB
# Check for firewall status
sudo ufw status
# Expected output: Firewall status (active/inactive)
# Expected output: 4 packets transmitted, 4 received
# Check for SELinux status
sestatus
# Expected output: SELinux status (enabled/disabled)
# Expected output: List of audio playback devices
# Check for system logs
dmesg | tail -n 20
# Expected output: Last 20 lines of system logs
# Expected output: List of Bluetooth devices if available
# Check for user permissions
id
# Expected output: User ID and group information
# Expected output: List of configured printers if available
# Check for environment variables
printenv
# Expected output: List of environment variables
# Expected output: List of video capture devices if available
# Check for running services
systemctl list-units --type=service --state=running
# Expected output: List of running services
# Expected output: No errors
# Check for disk I/O performance
dd if=/dev/zero of=/tmp/testfile bs=1M count=100 conv=fdatasync,notrunc
# Expected output: Disk write speed
# Expected output: No errors if clipboard is accessible
# Check for CPU load
top -b -n1 | head -n 10
# Expected output: Top 10 processes by CPU usage
# Expected output: Keyboard control settings if accessible
# Check for memory usage
free -m
# Expected output: Memory usage in MB
# Expected output: Pointer control settings if accessible
# Check for running daemons
ps aux | grep -E "(dbus|NetworkManager|pulseaudio)"
# Expected output: List of running daemons if available
# Expected output: List of touchscreen devices if available
# Check for system uptime
uptime
# Expected output: System uptime information
# Expected output: List of connected monitors if available
# Check for installed packages
dpkg -l | grep code
# Expected output: List of installed VS Code packages if any
# Expected output: List of available updates if any
# Check for GPU driver installation
lspci -k | grep -A 2 -i vga
# Expected output: GPU driver information if available
# Expected output: Firewall status (active/inactive)
# Check for sound card drivers
lspci -k | grep -A 2 -i audio
# Expected output: Sound card driver information if available
# Expected output: SELinux status (enabled/disabled)
# Check for Bluetooth drivers
